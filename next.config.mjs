/** @type {import('next').NextConfig} */
const nextConfig = {
  // We can leave this empty for now, or add specific settings as needed.
  // The 'turbo' boolean caused your crash, so we removed it.
};

export default nextConfig;
